name: fuelnearme
version: "1.0.0"
config-version: 2

profile: fuelnearme

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  fuelnearme:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
